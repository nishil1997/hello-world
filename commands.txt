git clone www.
git add x.txt
git add -A
git commit -m "committed message" x.txt   //one file
git commit -a -m "committed message"    //all files
git push
git push -u origin master
git pull
git remote add origin master (url)


FLOW:

pull changes made by others
edit
add
commit
push


git branch  (default master)
git branch newbranch   (makes a new branch  newbranch with all copy of master code) 
git checkout newbranch   (change branch to master now any chnages made are not copied to master)

